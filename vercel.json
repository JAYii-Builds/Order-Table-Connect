{
  "buildCommand": "pnpm --filter @workspace/restaurant-app run build",
  "outputDirectory": "artifacts/restaurant-app/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "build": {
    "env": {
      "NODE_VERSION": "24",
      "BASE_PATH": "/"
    }
  }
}
